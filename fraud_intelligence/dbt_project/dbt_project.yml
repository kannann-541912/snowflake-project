name: 'fraud_intelligence'
version: '1.0.0'
config-version: 2

profile: 'fraud_intelligence'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"
